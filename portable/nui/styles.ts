/**
 * Shared nucleify-ui CSS — safe to import on server (no custom element registration).
 * Fonts are loaded separately via `setupNui({ loadFonts: true })` or shell-specific modules.
 */
import 'nucleify-ui/styles/variables.css'
import 'nucleify-ui/styles/global.css'
import './tokens.css'
