---
description: Nuxt composables and shared request helpers (web)
globs: web/src/composables/**,web/src/**/use_*.ts,shared_modules/*/utils/api.ts
alwaysApply: false
---
# Composables — web

Auto-imported from `web/src/composables/**` (Nuxt auto-import).

Runtime helpers: `useConfig()`, `apiUrl()`, `appUrl()`, `appEnv()` (if present).

Shared request helpers live in `shared_modules/nuc_*/utils/api.ts` — import directly or re-export via `nucleify` alias.
