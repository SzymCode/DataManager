# <img src="/public/img/technologies/vitest.svg" width="25"> &nbsp;Vitest

This folder contains [Vitest](https://vitest.dev/guide/) tests. **We can use constants, types and utils via the `nucleify` alias (modules barrel).**

We can also put Vitests inside modules folders!