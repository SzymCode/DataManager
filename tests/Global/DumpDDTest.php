<?php

describe('no dump and dd', function() {
    test('in app')->todo();
    test('in database')->todo();
    test('in resources')->todo();
    test('in routes')->todo();
    test('in tests')->todo();
});
