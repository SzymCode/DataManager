<?php

describe('200', function() {
    test('authorized creates token')->todo();
    test('unauthorized token not redirect auth routes')->todo();
});

describe('302', function() {
    test('authorized token redirects auth routes')->todo();
});
