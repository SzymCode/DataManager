# web-next

Product shell for **web** on Next (`web-next/`). Scaffolded by tryb B:

```bash
make web TARGET=next
# or
pnpm compiler -- convert web --target=next
```

Canonical Nuxt app stays at top-level `web/`.
