export default function AdminStubPage() {
  return (
    <main style={{ fontFamily: 'system-ui', padding: '2rem' }}>
      <h1 style={{ margin: 0 }}>Nucleify Admin (Next)</h1>
      <p style={{ margin: '0.75rem 0 0', opacity: 0.7 }}>
        Run <code>convert admin --target=next</code> to emit React TSX from the Nuxt admin page.
      </p>
    </main>
  )
}
