# admin-next

Product shell for **admin** on Next (`admin-next/`). Scaffolded by tryb B:

```bash
make admin TARGET=next
# or
pnpm compiler -- convert admin --target=next
```

Canonical Nuxt app stays at top-level `admin/`.
